import React from 'react';

import './MovieSearchResultTemplate.css';

const MovieSearchResultTemplate = ({ children }) => (
  <div className="MovieSearchResultTemplate">
    {children}
  </div>
);

export default MovieSearchResultTemplate;
